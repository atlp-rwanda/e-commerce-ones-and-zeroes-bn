// import express from 'express';
// import { SearchController } from '../controllers/productSearch';

// const router = express.Router();

// // router.get('/:searchKeyword', SearchController.search);

// export default router;
