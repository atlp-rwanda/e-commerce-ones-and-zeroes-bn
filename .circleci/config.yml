version: 2.1

orbs:
  coveralls: coveralls/coveralls@2.2.1

jobs:
  build:
    docker:
      - image: circleci/node:14

    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
          environment: 
          COVERALLS_REPO_TOKEN :'xn8gurcoyZzAAFZxglDnesNmn9ziMmi6J';
      - run:
          name: Run tests
          command: npm test
      - run: 
          name: code-coverage
          command: npm run coverage
      - coveralls/upload   

workflows:
  version: 2
  build:
    jobs:
      - build
